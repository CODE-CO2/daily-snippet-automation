name: Daily Snippet Uploader

on:
  push:
    paths:
      - "snippets/**"            # snippets/ 안에 파일이 생기면 자동 업로드
  schedule:
    - cron: "0 0 * * *"          # 매일 00:00 UTC (KST 09:00)
  workflow_dispatch:              # 수동 실행용 입력값
    inputs:
      date:
        description: "업로드 날짜 (YYYY-MM-DD, 비우면 오늘 KST)"
        required: false
      content:
        description: "직접 업로드할 스니펫 내용(선택)"
        required: false
      user_email:
        description: "업로드 대상 사용자 이메일(선택, 미입력 시 매핑/기본값 사용)"
        required: false
      team_name:
        description: "팀 이름(선택, 미입력 시 기본값 사용)"
        required: false

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}        # 예: https://gqfegtdjewnadcmksktg.supabase.co
  SUPABASE_APIKEY: ${{ secrets.SUPABASE_APIKEY }}  # anon 키 또는 적합한 키
  SUPABASE_AUTH: ${{ secrets.SUPABASE_AUTH }}      # service role 또는 적합한 Bearer 토큰

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Prepare jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Pick date/content/user/team
        id: pick
        shell: bash
        run: |
          set -euo pipefail

          # 1) 날짜(KST, 기본 오늘)
          if [[ -n "${{ github.event.inputs.date || '' }}" ]]; then
            DATE="${{ github.event.inputs.date }}"
          else
            DATE="$(TZ=Asia/Seoul date +%F)"
          fi
          echo "date=$DATE" >> "$GITHUB_OUTPUT"

          # 2) 내용: 입력값>snippets/YYYY-MM-DD.md>기본 문구
          if [[ -n "${{ github.event.inputs.content || '' }}" ]]; then
            CONTENT="${{ github.event.inputs.content }}"
          else
            FILE="snippets/${DATE}.md"
            if [[ -f "$FILE" ]]; then
              CONTENT="$(cat "$FILE")"
            else
              CONTENT="(자동 업로드) ${DATE}의 스니펫 파일이 없습니다."
            fi
          fi

          # JSON에 안전하게 넣도록 escape
          CONTENT_JSON="$(printf '%s' "$CONTENT" | jq -Rs '.')"
          echo "content_json=$CONTENT_JSON" >> "$GITHUB_OUTPUT"

          # 3) 업로드 대상 이메일:
          # (우선순위) 입력값 > COLLABORATORS_JSON 매핑 > DEFAULT_USER_EMAIL
          USER_EMAIL_INPUT="${{ github.event.inputs.user_email || '' }}"
          if [[ -n "$USER_EMAIL_INPUT" ]]; then
            USER_EMAIL="$USER_EMAIL_INPUT"
          else
            # secrets.COLLABORATORS_JSON 예:
            # {"eun-ho1":"eq.jeh0224@gachon.ac.kr","teammate1":"abc@domain.com"}
            if [[ -n "${{ secrets.COLLABORATORS_JSON || '' }}" ]]; then
              GH="${{ github.actor }}"
              USER_EMAIL="$(jq -r --arg gh "$GH" '.[ $gh ] // empty' <<< '${{ secrets.COLLABORATORS_JSON }}')"
            fi
            if [[ -z "${USER_EMAIL:-}" ]]; then
              USER_EMAIL="${{ secrets.DEFAULT_USER_EMAIL }}"
            fi
          fi
          if [[ -z "${USER_EMAIL:-}" ]]; then
            echo "::error::USER_EMAIL을 결정할 수 없습니다. user_email 입력 또는 매핑/기본값을 설정하세요."
            exit 1
          fi
          echo "user_email=$USER_EMAIL" >> "$GITHUB_OUTPUT"

          # 4) 팀 이름: 입력값 > 기본값
          if [[ -n "${{ github.event.inputs.team_name || '' }}" ]]; then
            TEAM_NAME="${{ github.event.inputs.team_name }}"
          else
            TEAM_NAME="${{ secrets.DEFAULT_TEAM_NAME }}"
          fi
          if [[ -z "${TEAM_NAME:-}" ]]; then
            TEAM_NAME="default-team"
          fi
          echo "team_name=$TEAM_NAME" >> "$GITHUB_OUTPUT"

      - name: Validate secrets
        shell: bash
        run: |
          set -e
          [[ -n "$SUPABASE_URL" ]] || { echo "::error::SUPABASE_URL이 비어 있습니다"; exit 1; }
          [[ -n "$SUPABASE_APIKEY" ]] || { echo "::error::SUPABASE_APIKEY가 비어 있습니다"; exit 1; }
          [[ -n "$SUPABASE_AUTH" ]]  || { echo "::error::SUPABASE_AUTH가 비어 있습니다"; exit 1; }
          echo "✅ Secrets OK"

      - name: Upload to Supabase (REST)
        id: call
        shell: bash
        run: |
          set -euo pipefail

          API="${SUPABASE_URL%/}/rest/v1/snippets?select=*"

          # payload 조립(모든 값 JSON-safe)
          PAYLOAD="$(jq -n \
            --arg d   "${{ steps.pick.outputs.date }}" \
            --arg em  "${{ steps.pick.outputs.user_email }}" \
            --arg tn  "${{ steps.pick.outputs.team_name }}" \
            --argjson ct ${{ steps.pick.outputs.content_json }} \
            '{snippet_date:$d, user_email:$em, team_name:$tn, content:$ct}')"

          echo "🔎 Endpoint: $API"
          echo "🔎 Payload: $PAYLOAD"

          # 요청
          RESP="$(curl -sS -w '\nHTTP_CODE:%{http_code}\n' -X POST "$API" \
            -H "apikey: $SUPABASE_APIKEY" \
            -H "Authorization: Bearer $SUPABASE_AUTH" \
            -H "Content-Type: application/json" \
            -H "Prefer: return=representation" \
            -d "$PAYLOAD")"

          echo "$RESP"

          CODE="$(grep -a 'HTTP_CODE:' <<< "$RESP" | tail -n1 | cut -d: -f2)"
          if [[ "$CODE" != 200 && "$CODE" != 201 ]]; then
            echo "::error::업로드 실패 (HTTP $CODE)"
            exit 1
          fi
          echo "✅ 업로드 성공 (HTTP $CODE)"
